## Day 3 复盘
### 今天我实际学会了什么？
- 掌握了通过 Python SDK 调用 LLM API 的完整流程。
- 搞懂了 `.env` 保护 API Key 的重要性和方法。
- 深刻理解了 `messages` 列表中 `system` 和 `user` 角色的作用。
- 搞懂了怎么从复杂的 API 返回结构（`choices[0].message.content`）中剥离出最终的文本。

### 今天卡住的点是什么？
- 对 API 返回的复杂结构一开始有点懵，后来懂了它是嵌套对象。

### 明天最重要的一件事是什么？
- 让大模型拥有“记忆”！尝试编写多轮对话，让它记住我上一句说的话。